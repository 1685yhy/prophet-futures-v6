# ── 核心 AI / LangChain ────────────────────────────────────────────────────
langchain==1.3.4
langchain-core==1.4.1
langchain-anthropic==1.4.4
langgraph==1.2.4
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
anthropic==0.107.1

# LLM 提供商（二选一，推荐 Anthropic）
# langchain-openai>=0.1.0      # 如需使用 OpenAI，取消此行注释

# ── 数据 ───────────────────────────────────────────────────────────────────
akshare==1.18.64
pandas==2.2.1
numpy==1.26.4
scikit-learn>=1.3.0
xgboost>=2.0.0
pydantic==2.12.5
pydantic_core==2.41.5
PyYAML==6.0.3
python-dotenv==1.0.1

# ── 技术图表（视觉 Agent）─────────────────────────────────────────────────
matplotlib==3.10.8

# ── 因果图 ─────────────────────────────────────────────────────────────────
networkx==3.6.1

# ── 类型系统 ───────────────────────────────────────────────────────────────
typing_extensions==4.15.0

# ── 测试 ───────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0

# ── 可选依赖（缺失时系统自动降级为规则 fallback）──────────────────────────
# chromadb>=0.5.0          # 向量记忆库（推荐）：pip install chromadb
# river>=0.21.0            # 在线学习/漂移检测：pip install river
# gymnasium>=0.29.0        # RL 执行环境：pip install gymnasium
# langchain-openai>=0.1.0  # OpenAI 支持：pip install langchain-openai openai
